{
  "name": "finflow-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "FinFlow mobile touch regression E2E test suite (Playwright). Test-only tooling; does not affect the app build.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:mock": "playwright test --config=playwright.mock.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
